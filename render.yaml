services:
  - type: web
    name: academic-resource-system
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.16.0
      - key: MONGO_URI
        sync: mongodb+srv://mahi:1234mahi@cluster0.hmu4jd1.mongodb.net/arus?retryWrites=true&w=majority&appName=Cluster0
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
      - key: SESSION_SECRET
        generateValue: true
      - key: PORT
        value: 10000
